import React from "react";
import { useState, useEffect } from "react";

const Products = () => {
  const [prev, usePrev] = useState("");

  const handleClick = (e) => {
    console.log(usePrev);
  };
  const usePrev = 

  return <div>Products</div>;
};

export default Products;
